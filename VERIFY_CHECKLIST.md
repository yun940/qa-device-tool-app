# QA 디바이스 대여 툴 — 변경 검증 체크리스트

PC 웹(`index.html`) / 모바일 앱(`app.html` · APK · iOS PWA) / GAS 백엔드 / 카카오워크 알림 전체 라이프사이클을 한 사이클 안에 수동으로 돌려보는 체크리스트. 코드 변경(특히 백엔드 액션 추가, 사용자/바인딩 시트 변경, 카카오워크 메시지 형식 변경, 모바일 흐름 변경) 후 30분 안에 회귀 점검을 끝내는 용도로 사용한다. 각 시나리오는 `- [ ]` 박스를 손으로 체크하면서 진행한다.

---

## 사전 준비

### 스크립트 속성 (테스트 모드)
| 키 | 값 | 의미 |
|---|---|---|
| `RENTAL_DURATION_MIN` | `3` | 대여 기간 3분 |
| `PRE_ALERT_BEFORE_MIN` | `1` | 만료 1분 전 사전 알림 |
| `OVERDUE_INTERVAL_MIN` | `1` | 연체 알림 1분 간격 (최대 3회) |
| `AUTO_RETURN_AFTER_MIN` | `4` | 만료 후 4분 경과 시 자동 반납 |
| `ENFORCE_HOUR` | `0` | 시간 제약 해제 (10시/12시 외에도 발화) |
| `KAKAOWORK_WEBHOOK_URL` | (실 채널) | 검증용 카카오워크 채널 |
| `RENTAL_STATUS_URL` | `https://yun940.github.io/qa-device-tool-app/` | 알림 버튼 링크 |

- [ ] GAS 에디터 → 프로젝트 설정 → 스크립트 속성에서 위 값이 모두 세팅됨
- [ ] GAS 에디터에서 `installExpiryTrigger` 1회 실행 (1분 주기 트리거)
- [ ] `initialSetup` 실행해 4개 시트(`대여기록`, `디바이스목록`, `사용자목록`, `디바이스바인딩`) 생성 확인

### 시드 데이터
- [ ] `사용자목록` 시트 (헤더: 아이디 / 이름 / 카카오워크이메일 / 권한 / 푸시토큰 / 최근로그인)
  - `0000` / `관리자` / (이메일) / `admin` / (빈칸) / (빈칸)
  - `iswony` / `우원택` / (이메일) / `user` / (빈칸) / (빈칸)
- [ ] `디바이스목록` 시트 — 컬럼별 카테고리, 검증용 디바이스 2개 이상 (예: `iPhone 15 Pro`, `Galaxy S24`)
- [ ] `디바이스바인딩` 시트는 비어 있음 (헤더만)
- [ ] `대여기록` 시트는 빈 상태 또는 모든 행에 반납일시 채워짐

### 클라이언트
- [ ] `js/config.js`의 `WEB_APP_URL`이 테스트 GAS 배포 URL을 가리킴
- [ ] 검증용 폰 2대 준비 (A: 일반 사용자, B: 관리자 또는 다른 일반 사용자)
- [ ] PC 브라우저에서 `index.html` 열어 둠 (현황 모니터링용)

---

## 시나리오 1: 신규 사용자 + 신규 폰 첫 사용

폰 A에서 `app.html` 첫 실행 → 로그인 → 디바이스 등록 → 대여 → 반납(자동 로그아웃) 한 사이클.

| 단계 | 액션 | 예상 시트 변화 | 예상 카카오워크 알림 |
|---|---|---|---|
| 1 | 폰 A에서 `app.html` (또는 APK) 실행 | — | — |
| 2 | 아이디 `iswony` 입력 → 로그인 | `사용자목록` 해당 행 `최근로그인` 갱신 | — |
| 3 | 디바이스 등록 화면이 자동 표시되는지 확인 (드롭다운에 디바이스 목록 노출, 다른 폰 바인딩분은 숨김) | — | — |
| 4 | 드롭다운에서 `iPhone 15 Pro` 선택 → 등록 | `디바이스바인딩`에 새 행 추가 (고유ID / iPhone 15 Pro / `ios` 또는 `web` / 모델코드 / 등록일시) | — |
| 5 | 메인 화면에 `대여하기` 버튼 노출 확인 | — | — |
| 6 | `대여하기` → 셀(예: `1셀`) 선택 → 확인 | `대여기록`에 1행 추가 (반납일시 빈칸, 만료일시=현재+3분, 알림단계=0) | `[대여] iPhone 15 Pro — 우원택 @우원택 (1셀)` 단건 (header: 디바이스 대여, blue) |
| 7 | 메인 화면이 `연장하기` + `반납하기` 버튼으로 갱신되는지 | — | — |
| 8 | `반납하기` → 확인 | 해당 행 반납일시 채워짐 | `[반납] iPhone 15 Pro — 우원택 @우원택 (1셀)` 단건 (header: 디바이스 반납, yellow) |
| 9 | 1.8초 후 자동으로 로그인 화면으로 복귀 | — | — |

체크
- [ ] 로그인 → 등록 → 대여 → 반납 → 로그아웃까지 끊김 없이 진행
- [ ] 반납 후 정확히 1.8초 안내 후 자동 로그아웃 (다음 사용자 인계 가능 상태)
- [ ] 모바일 화면이 1열 그리드로 보이고 가로 절반만 보이는 회귀 없음
- [ ] PC `index.html`에서 같은 디바이스가 대여 중 → 반납 상태로 즉시 반영됨 (페이지 새로고침 없이도 폴링으로 갱신)

---

## 시나리오 2: 만료 / 연체 / 자동 반납 라이프사이클

테스트 모드(3/1/1/4분) 기준으로 t=0에 대여 → t+7까지 7분 안에 전체 흐름 확인. 대여 시각을 메모해 두고 분 단위로 체크.

| 시각 | 액션 / 상태 | 예상 시트 변화 (`대여기록`) | 예상 카카오워크 알림 |
|---|---|---|---|
| t+0 | 폰 A에서 대여 (셀=1셀) | 새 행 추가 — 만료일시=t+3, 알림단계=0 | `[대여] …` 단건 (blue) |
| t+2 | (만료 1분 전) 1분 트리거에서 사전 알림 발화 | 마지막알림=현재시각, 알림단계=1 | `⏰ 곧 만료 갱신 필요 (1건)` batch 메시지 (yellow). description.term=디바이스명, content="우원택 @우원택 · 셀 1셀 · 만료 …" |
| t+3 | 만료 시점 | (변화 없음 — 트리거 다음 주기에서 잡힘) | — |
| t+4 | 연체 1일차 알림 | 알림단계=2 | `🚨 연체 발생 (1건)` batch (red). content에 "1일차" 표기 |
| t+5 | 연체 2일차 | 알림단계=3 | `🚨 연체 발생 (1건)` content "2일차" |
| t+6 | 연체 3일차 | 알림단계=4 | `🚨 연체 발생 (1건)` content "3일차" |
| t+7 | 만료 +4분 경과 → 자동 반납 | 같은 행 반납일시 채워짐 | `⚠️ 자동 반납 처리 (1건)` batch (red), content "… · 셀 1셀 · 반납 …" |

체크
- [ ] 사전 알림은 **1회만** 발화 (다음 1분 트리거에서 중복 안 됨)
- [ ] 연체 알림은 1, 2, 3일차가 각각 1회씩만 발화 (총 3회)
- [ ] 자동 반납 후에는 연체/사전 알림 추가 발화 없음
- [ ] PC `index.html`에서 페이지 유지한 상태로 만료 → 연체 태그가 자동 갱신됨 (새로고침 불필요)
- [ ] 자동 반납된 행이 `반납일시` 컬럼에 자동 반납 시각으로 기록됨
- [ ] 카카오워크 메시지 본문에 `@우원택` 멘션이 description.content와 fallback text에 모두 포함

### 동시 대여 2건일 때 묶음 검증 (선택)
- [ ] 폰 A, B에서 거의 동시에 다른 디바이스를 대여 → t+2~t+4 사이 사전/연체 알림이 **종류별 1개 메시지에 2건**으로 묶여 발송됨 (`⏰ 곧 만료 갱신 필요 (2건)`)

---

## 시나리오 3: 권한별 분기 (관리자 강제 반납)

| 단계 | 액션 | 예상 시트 변화 | 예상 카카오워크 알림 |
|---|---|---|---|
| 1 | 폰 A(`iswony`)로 디바이스 X 대여 중 상태 만듦 | 대여기록 행 1건 | `[대여] …` |
| 2 | 폰 B에서 로그아웃 상태로 `iswony` 외 일반 사용자로 로그인 후 디바이스 X 폰으로 접근 | — | — |
| 3 | 메인 화면에 안내 메시지(타인 대여 중)만 표시, **버튼 없음** 확인 | — | — |
| 4 | 폰 B에서 `0000` 관리자 로그인 후 디바이스 X 폰으로 접근 | — | — |
| 5 | `관리자 반납 (강제)` 빨간 버튼 노출 확인 | — | — |
| 6 | 강제 반납 진행 | 해당 행 반납일시 채워짐 (관리자 시각) | `[반납] …` 단건 (반납자는 원 대여자 이름 유지) |
| 7 | 강제 반납 직후 **자동 로그아웃되지 않음** (관리자는 여러 건 연속 처리) | — | — |

체크
- [ ] 일반 사용자가 타인 대여 중 화면에서 버튼이 노출되지 않음 (회귀 핵심)
- [ ] 관리자 화면의 빨간 강제 반납 버튼은 일반 사용자 권한에는 절대 노출되지 않음
- [ ] 관리자 강제 반납 후 로그아웃 없이 메인 화면 유지

---

## 시나리오 4: 동일 디바이스 중복 등록 방지

| 단계 | 액션 | 예상 결과 |
|---|---|---|
| 1 | 폰 A에서 디바이스 X를 정상 바인딩 (시나리오 1의 4단계 결과) | `디바이스바인딩`에 폰A 행 1개 |
| 2 | 폰 B에서 로그인 후 등록 화면 열기 | 드롭다운에 디바이스 X가 **표시되지 않음**, 화면 하단에 "다른 폰에 이미 등록된 1개는 숨김" 안내 |
| 3 | 폰 B에서 디바이스 Y 선택 후 등록 | `디바이스바인딩` 폰B 행 추가 |
| 4 | (회귀) 폰 A에서 다시 등록 화면 진입 시 X 옆에 `· 현재 등록됨` 라벨 표시 | — |

체크
- [ ] 다른 폰이 바인딩한 디바이스는 신규 폰의 드롭다운에서 완전히 숨김
- [ ] 본인 폰이 이미 바인딩한 디바이스는 라벨(`· 현재 등록됨`) 표시
- [ ] `디바이스바인딩` 시트에 동일 디바이스명이 두 폰의 고유ID로 동시에 들어가지 않음

---

## 시나리오 5: 카카오워크 알림 취합 (1회 트리거 = 1개 메시지)

`checkExpiringRentals` 트리거가 1회 실행될 때 종류별로 묶어 발송되는지 검증. 시나리오 2와 함께 진행해도 됨.

준비
- [ ] 폰 A, B에서 각각 다른 디바이스를 거의 동시(±10초)에 대여, 동일한 분에 만료되도록 셋업

| 트리거 시점 | 예상 알림 (개수) |
|---|---|
| 사전 알림 시점 | `⏰ 곧 만료 갱신 필요 (2건)` **1개 메시지** — description 블록 2개 |
| 연체 1일차 시점 | `🚨 연체 발생 (2건)` 1개 메시지 |
| 자동 반납 시점 | `⚠️ 자동 반납 처리 (2건)` 1개 메시지 |

체크
- [ ] 동일 종류 알림이 같은 트리거에서 분할 발송되지 않음 (1건 메시지에 N개 description)
- [ ] 서로 다른 종류는 분리 발송 (예: 같은 트리거에 사전알림 1건 + 연체 1건이 함께 잡히면 2개 메시지)
- [ ] 사용자 액션(대여/반납/갱신)은 즉시 단건 메시지 (배치 X)
- [ ] 모든 메시지 하단에 `대여 현황 보기` 버튼 노출 (RENTAL_STATUS_URL 설정 시)

---

## 시나리오 6: 다중 환경에서 동일 디바이스 식별 일관성

같은 폰을 (a) Capacitor APK, (b) iOS Safari → 홈 화면 PWA, (c) PC 데스크탑 브라우저로 각각 접속했을 때 고유ID가 다르게 잡히는지/같게 잡히는지 확인.

| 환경 | 고유ID 출처 | 예상 동작 |
|---|---|---|
| Android APK | `@capacitor/device` `getId().identifier` (ANDROID_ID) | 같은 폰에서 매번 동일 |
| iOS PWA (Safari → 홈 화면) | `localStorage` 기반 가짜 고유ID (현재 IPA 미빌드, 디버그 안내 표시) | 같은 PWA 인스턴스 내에서 동일, Safari 일반 탭과는 별도 |
| 데스크탑 브라우저 | `localStorage` 기반 가짜 고유ID | 같은 브라우저 프로필 내 동일, 시크릿 모드는 새 ID |

체크
- [ ] APK 재설치 시 ANDROID_ID는 그대로 유지 (앱 데이터 초기화 시에만 변동 가능)
- [ ] iOS PWA에서 디버그 정보의 OS 버전 자리에 "iOS (정확한 버전은 IPA 빌드 시 표시)" 안내 노출
- [ ] 데스크탑 브라우저 시크릿 모드에서 새 고유ID 생성되어 신규 등록 화면 진입 (기존 바인딩에 영향 없음)
- [ ] 같은 환경에서 새로고침/재실행해도 `resolveDevice` 응답의 `bound: true`가 유지 (재등록 강제되지 않음)

---

## 회귀 체크 항목

### UI (frontend)
- [ ] 모바일 `index.html`에서 `device-list-main` 그리드가 1열로 표시 (가로 반쪽 잘림 없음)
- [ ] 만료/연체 태그가 페이지 유지 상태에서 자동 갱신 (폴링 동작)
- [ ] QR 이미지가 일정 주기로 자동 갱신 (`qr-generator.html`)
- [ ] 접근 게이트(`js/access-gate.js`) 세션 유지 — 정상 통과 후 새로고침 시 재진입 강제 안 됨
- [ ] `app.html` 로그인 화면에서 아이디 미입력 시 안내 메시지 표시
- [ ] 등록 화면 드롭다운에 카테고리 단락이 보임 (혹은 평탄화된 디바이스명 리스트)
- [ ] 본인 반납 직후 1.8초 안내 → 자동 로그아웃 (시나리오 1)
- [ ] 관리자 강제 반납 시 자동 로그아웃 없음 (시나리오 3)

### 백엔드 (gas-backend)
- [ ] `대여기록` 시트 10개 컬럼 유지 (번호/디바이스ID/디바이스명/대여자/셀/대여일시/반납일시/만료일시/마지막알림/알림단계)
- [ ] `사용자목록` 시트 6개 컬럼 유지
- [ ] `디바이스바인딩` 시트 5개 컬럼 유지
- [ ] `installExpiryTrigger` 1분 주기 트리거 1개만 등록 (중복 트리거 없음)
- [ ] 만료/연체/자동반납 알림 단계 전이가 0 → 1 → 2 → 3 → 4 순서대로만 진행 (역행/중복 없음)
- [ ] `findCurrentRental`이 반납일시 비어있는 가장 최근 행을 정확히 가져옴
- [ ] `bindDevice`: 같은 `uniqueId` 재호출 시 기존 행 업데이트 (행 중복 추가 X)
- [ ] `resolveDevice` 응답에 `currentRental` 필드가 대여 중일 때만 채워짐

### 알림 (notification)
- [ ] 사용자 액션(rent/return/renew)은 즉시 단건 메시지
- [ ] 시스템 알림(preExpiry/overdue/autoReturn)은 1회 트리거당 종류별 1개 메시지(배치)
- [ ] description.term=디바이스명, content=`{대여자} @{멘션} · 셀 X · 만료 …` 포맷 유지
- [ ] header 색상: rent=blue, return=yellow, renew=blue, preExpiry=yellow, overdue=red, autoReturn=red
- [ ] fallback text에 멘션 텍스트 포함 (알림 미리보기에서도 멘션 보임)
- [ ] `대여 현황 보기` 버튼이 `RENTAL_STATUS_URL`로 외부 브라우저 오픈

---

## 트러블슈팅 빠른 참조

| 증상 | 1차 조치 |
|---|---|
| 카카오워크 알림 미수신 | 스크립트 속성 `KAKAOWORK_WEBHOOK_URL` 확인 → GAS 에디터에서 `testKakaoWorkPlain` 실행해 200 응답 확인 |
| 사전/연체 알림이 한참 늦게 옴 | 트리거가 `everyMinutes(1)`로 설치되었는지 확인. `ENFORCE_HOUR=1`이면 10시/12시에만 발화하므로 테스트 중에는 `0`으로 |
| 자동 반납이 안 됨 | `AUTO_RETURN_AFTER_MIN`이 너무 큰지 확인. `대여기록`의 `반납일시`가 이미 채워져 있으면 skip됨 |
| 등록 화면에 디바이스가 모두 숨김 | `디바이스바인딩` 시트에 다른 폰 바인딩이 모든 디바이스를 점유 중일 가능성. 시트에서 직접 행 삭제로 초기화 |
| 로그인 실패("등록되지 않은 아이디") | `사용자목록` 시트 1열(아이디) 정확히 매칭되는지(공백/대소문자) 확인 |
| 폰 A에서 본 디바이스명이 폰 B에서 다르게 표시 | 폰 A가 바인딩할 때 `deviceName` 파라미터가 `디바이스목록` 시트 값과 정확히 같은지 확인. 불일치 시 `bindDevice`가 거부 |
| 모바일에서 가로 반쪽만 보임 | `css/style.css`의 `device-list-main` 모바일 브레이크포인트 1열 그리드 회귀. `index.html`에 viewport meta 누락 가능성 |
| PC 페이지에서 만료 태그가 갱신 안 됨 | `js/app.js` 폴링 인터벌 동작 여부 / `getAllDeviceStatus` 응답에 만료 정보 포함되는지 확인 |
| 자동 반납이 두 번 발화 | 트리거가 2개 이상 설치된 케이스. `uninstallExpiryTrigger` 후 `installExpiryTrigger` 1회 |
| 같은 폰이 매번 신규 등록 화면 | `resolveDevice` 응답 `bound: false`. `device-info.js`의 `getFakeUniqueId`/네이티브 `Device.getId()` 동작 확인 (localStorage 초기화 여부) |

---

검증 중 버그 발견 시 담당 에이전트로 위임:
- UI / `app.html` / `app-screen.js` 흐름 → `frontend-engineer`
- `Code.gs` 액션/시트/트리거 → `gas-backend-engineer`
- 카카오워크 메시지 형식 → `notification-designer`
